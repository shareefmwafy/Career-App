name: Build and SonarQube Analysis

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and Analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for better relevancy of analysis

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x' # Or the version you use in your project

      # Change to the Mobile directory
      - name: Change to Mobile directory
        run: cd Frontend/Mobile

      # Install project dependencies
      - name: Install dependencies
        run: npm install
        # working-directory: Frontend/Mobile  # Set working directory to Mobile

      # Optionally run tests
      - name: Run Tests
        run: npm test
        # working-directory: Frontend/Mobile  # Set working directory to Mobile

      # Run SonarQube scan
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v3
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        # working-directory: Frontend/Mobile  # Set working directory to Mobile

      # If you wish to fail the job when the Quality Gate is red, uncomment the
      # following lines. This is typically used to fail a deployment.
      # - name: SonarQube Quality Gate
      #   uses: sonarsource/sonarqube-quality-gate-action@master
      #   timeout-minutes: 5
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
